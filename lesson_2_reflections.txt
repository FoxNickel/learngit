1.初始化版本库时会发生什么情况？为什么你需要这样做？
answer：会创建.git文件，然后运行git status会提示你当前喂未commit的文件

2.暂存区与工作目录和版本库有何不同？你认为它具有什么价值？
暂存区可以用来将相关的文件add在一起，然后只进行一次commit。

3.如何使用暂存区确保为每项逻辑更改进行一次提交？
将属于一次逻辑提交的代码放进一次add中。

4.在哪些情况下，分支可以帮助你有序地组织历史记录？分支将如何提供帮助？


5.图表如何帮助你直观显示分支结构？
更直观

6.如果删除某个分支并导致无法从现有的分支访问一些提交，则在 Git 的垃圾收集过程运行之前，仍可继续通过提交 ID 访问这些提交。除非你主动关闭此过程，否则它将不时自动运行。也可以通过命令 git gc 手动运行此过程

7.将两个分支合并到一起有何结果？为什么我们在图表中表示它的方式是那样的？
会将非master分支的更改合并到master上

